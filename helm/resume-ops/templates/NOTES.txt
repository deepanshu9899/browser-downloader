1. Access ResumeOps frontend using:
{{- if .Values.frontend.ingress.enabled }}
  http{{ if .Values.frontend.ingress.tls.enabled }}s{{ end }}://{{ .Values.frontend.ingress.host }}
{{- else }}
  kubectl port-forward svc/{{ include "resume-ops.fullname" . }}-frontend 8080:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}
  then open http://localhost:8080
{{- end }}

2. API endpoints remain internal-only and are intended to be accessed through the frontend proxy.
